[tox]
envlist = py310
isolated_build = True

[testenv]
deps =
    pytest>=7.0
    pytest-cov
    PySide2
commands =
    pytest tests/ -v --cov=sumrope --cov-report=term-missing

[testenv:dev]
description = Run tests in development mode with current Python
deps =
    pytest>=7.0
    pytest-cov
    PySide2
commands =
    pytest tests/ -v

[testenv:lint]
description = Run linting checks
deps =
    mypy
    ruff
    PySide2
skip_install = True
commands =
    ruff check src/
    mypy src/

[testenv:format]
description = Format code
deps =
    ruff
skip_install = True
commands =
    ruff format src/ tests/
