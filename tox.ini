[tox]
envlist = py311
isolated_build = True

[testenv]
deps =
    pytest>=7.0
    pytest-cov
    pytest-qt>=4.0
    PySide6
setenv =
    QT_QPA_PLATFORM = offscreen
commands =
    pytest tests/ -v --cov=QCodeSitter --cov-report=term-missing

[testenv:dev]
description = Run tests in development mode with current Python
deps =
    pytest>=7.0
    pytest-cov
    pytest-qt>=4.0
    PySide6
setenv =
    QT_QPA_PLATFORM = offscreen
commands =
    pytest tests/ -v

[testenv:lint]
description = Run linting checks
deps =
    mypy
    ruff
    PySide6
skip_install = True
commands =
    ruff check src/
    mypy src/

[testenv:format]
description = Format code
deps =
    ruff
skip_install = True
commands =
    ruff format src/ tests/
